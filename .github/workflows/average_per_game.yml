name: Average Per Game

on:
  workflow_dispatch:  # Manual trigger only

permissions:
  contents: write  # 👈 gives Actions permission to push commits

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}  # uses your repo’s default token

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install pandas

      - name: Run averaging script
        run: python src/average_per_game.py

      - name: Commit and push results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/processed/stats/*.csv
          git commit -m "Update per-game averages" || echo "No changes to commit"
          git push
