usage: basketball_backtest.py [-h] [--backtest-dir BACKTEST_DIR]
                              [--model-config MODEL_CONFIG]
                              [--markets-config MARKETS_CONFIG]
                              [--model-source {dratings,sdv,ensemble}]
                              [--parity-rows PARITY_ROWS]
                              [--run-name RUN_NAME]

Replay combined historical basketball files through current downstream model
and selection logic.

options:
  -h, --help            show this help message and exit
  --backtest-dir BACKTEST_DIR
  --model-config MODEL_CONFIG
  --markets-config MARKETS_CONFIG
  --model-source {dratings,sdv,ensemble}
  --parity-rows PARITY_ROWS
  --run-name RUN_NAME
